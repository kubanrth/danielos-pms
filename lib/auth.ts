// Auth.js v5 configuration — populated in Faza 1.
// Kept minimal here so `lib/` imports work in Faza 0 smoke tests.
export const authPlaceholder = {
  // In Faza 1 export:
  //   auth, handlers, signIn, signOut
  // from NextAuth({...}) with Credentials provider + Prisma adapter.
  status: "scaffolded" as const,
};
